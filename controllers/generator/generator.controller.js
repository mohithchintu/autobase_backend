// import { route_generate } from "../../helpers/code_generators/main.js";

// export const generete_code = async (req, res) => {
//   const { method, url_path, func, lang } = req.body;
//   const code_status = route_generate({ method, url_path, func, lang });
//   res.status(200).json(code_status);
// };
